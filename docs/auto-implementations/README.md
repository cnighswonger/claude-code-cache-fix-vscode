# Implementation Documentation

This folder contains auto-generated feature documentation.
Files are organized by user-facing capability (e.g., auto-init.md,
ai-commit-messages.md). Each file describes what the feature does,
how it works, what settings control it, and which source files implement it.

Files are created and updated automatically by GitDocs every N commits.
